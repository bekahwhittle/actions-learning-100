name: hello-world

on: push

jobs:
  hello-world:

    runs-on: ubuntu-latest

    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "hello world 🗺️"